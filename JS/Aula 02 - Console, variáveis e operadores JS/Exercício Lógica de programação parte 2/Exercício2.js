let retangulo = 'retangulo'

const base = 5;

const altura = 10;

const area_do_retangulo = base * altura;

console.log (`Multiplicando a base ${base} com a altura ${altura} a área do ${retangulo} é ${area_do_retangulo}`)